# Data

Benchmark input tables should be stored under `data/`. The default benchmark config points to `data/benchmark/xgb_cap_50_sample.csv` as a one-row smoke-test input.

Replace the sample file with the manuscript prediction table or update `configs/benchmark.yaml` to reference the correct CSV.
